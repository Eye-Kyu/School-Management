// Re-exported from @school-manager/types — the canonical definition, shared
// with apps/web (which mints via this API but verifies independently for
// direct Supabase reads + middleware routing + the banner).
export { ASSIST_MODE_COOKIE, AssistModeClaims } from '@school-manager/types';

# App icons

Drop in `icon-192.png` and `icon-512.png` once you have a logo.

For now the PWA install will show a default browser placeholder - that's fine
for v0.1, but replace before any school sees it.

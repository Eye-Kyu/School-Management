# Seed data

CSV files that the seed script can consume to populate a demo school
with realistic-looking (but obviously fake) students, classes, and timetables.

Add files here later, e.g. `students.csv`, `classes.csv`. Names must be
clearly fake (`Test Student 001`, `+254700000001`) - never real-looking PII.
